server:
  port: ${PORT:8001}

base-url: /api

spring:
  application:
    name: taskManager

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  mvc:
    hiddenmethod:
      filter:
        enabled: true

rollbar_token: 24cfbd3e725c4c299d787ad0abc69d94
